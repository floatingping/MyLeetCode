/*
543. Diameter of Binary Tree
https://leetcode.com/problems/diameter-of-binary-tree/

Given the root of a binary tree, return its diameter: the number of edges on the longest path between any two nodes. The path does not have to pass through the root.

Example 1:

Input: root = [1,2,3,4,5]
Output: 3
Explanation: One longest path is 4 -> 2 -> 1 -> 3.

Example 2:

Input: root = [1,2]
Output: 1

Constraints:

The number of nodes is in the range [1, 10^4].
-100 <= Node.val <= 100
*/

/**
 * @param {TreeNode} root
 * @return {number}
 */
var diameterOfBinaryTree = function (root) {



};

console.log(diameterOfBinaryTree([1, 2, 3, 4, 5])); // 3
console.log(diameterOfBinaryTree([1, 2])); // 1
console.log(diameterOfBinaryTree([1])); // 0


// node Grind169/1-21.js
