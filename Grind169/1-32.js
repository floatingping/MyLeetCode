/*
Easy | 15 mins
136. Single Number
https://leetcode.com/problems/single-number/

Given a non-empty integer array where every value appears exactly twice except one value, return the value that appears once.

Example 1:

Input: nums = [2,2,1]
Output: 1
Explanation: 2 appears twice and 1 appears once.

Example 2:

Input: nums = [4,1,2,1,2]
Output: 4

Example 3:

Input: nums = [1]
Output: 1

Constraints:

1 <= nums.length <= 3 * 10^4
-3 * 10^4 <= nums[i] <= 3 * 10^4
Every element appears twice except for one element.
*/

/**
 * @param {number[]} nums
 * @return {number}
 */
var singleNumber = function (nums) {



};

console.log(singleNumber([2, 2, 1])); // 1
console.log(singleNumber([4, 1, 2, 1, 2])); // 4
console.log(singleNumber([1])); // 1


// node Grind169/1-32.js
